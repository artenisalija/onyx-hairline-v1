<aside class="floating-contact" aria-label="Kontakt i shpejtë">
  <!-- TODO: konfirmo numrin e kontaktit të Onyx Hairline -->
  <a class="float-action float-action--whatsapp" href="https://wa.me/355XXXXXXXXX" target="_blank" rel="noopener noreferrer" aria-label="Na shkruani në WhatsApp">
    <span>Na Shkruani</span>
    <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M20 11.6a8 8 0 0 1-11.8 7L4 20l1.4-4A8 8 0 1 1 20 11.6Z" stroke="currentColor" stroke-width="1.6"/><path d="M9 8.5c.3 2.7 2 4.4 4.7 4.8" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>
  </a>
  <a class="float-action" href="tel:+355XXXXXXXXX" aria-label="Na telefononi">
    <span>Na Telefononi</span>
    <svg viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M7.2 3.5 10 7.2 8.4 9.4c1 2.5 3 4.4 5.4 5.4l2.2-1.6 3.8 2.8c.4.3.6.8.4 1.2-.7 2-2.3 3.2-4.3 3-6.7-.8-11.4-5.5-12.2-12.2-.2-2 1-3.6 3-4.3.2-.1.4-.1.5-.2Z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg>
  </a>
</aside>

<style>
  .floating-contact { position: fixed; z-index: 40; right: 1.25rem; bottom: 1.25rem; display: flex; flex-direction: column; align-items: flex-end; gap: .55rem; }
  .float-action { display: flex; min-width: 3.25rem; height: 3.25rem; align-items: center; justify-content: flex-end; gap: .75rem; padding: 0 .95rem; border: 1px solid rgba(242,234,223,.25); border-radius: 999px; background: rgba(18,17,15,.9); box-shadow: 0 8px 30px rgba(0,0,0,.28); backdrop-filter: blur(10px); color: var(--ivory); transition: border-color .25s ease, background-color .25s ease, transform .25s ease; }
  .float-action:hover { border-color: var(--copper); background: var(--copper); color: var(--onyx); transform: translateX(-3px); }
  .float-action svg { width: 1.25rem; flex: none; }
  .float-action span { max-width: 0; overflow: hidden; font-size: .63rem; font-weight: 700; letter-spacing: .1em; opacity: 0; text-transform: uppercase; white-space: nowrap; transition: max-width .35s ease, opacity .25s ease; }
  .float-action:hover span, .float-action:focus-visible span { max-width: 8rem; opacity: 1; }
  .float-action--whatsapp { background: var(--ivory); color: var(--onyx); }
  @media (max-width: 600px) {
    .floating-contact { right: .75rem; bottom: .75rem; flex-direction: row; }
    .float-action { width: 3.1rem; height: 3.1rem; min-width: 3.1rem; padding: 0; justify-content: center; }
    .float-action span { display: none; }
  }
</style>
